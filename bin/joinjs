#!/usr/bin/env common-node
process.argv.shift();
process.argv.shift();
var filename = process.argv.shift();
  if(filename) {
  var output = require('../lib/index').joinjs(filename);
  var destination = process.argv.shift();
  if(destination) {
    fs.write(destination, output);
  } else {
    console.log(output);
  }
} else {
  console.log('usage: joinjs dir/main');
}